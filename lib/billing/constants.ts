/**
 * Billing constants - Single source of truth
 *
 * These values are used across Next.js and Convex code.
 * Change them here, they propagate everywhere.
 */

/** Trial duration in milliseconds (14 days) */
export const TRIAL_DURATION_MS = 14 * 24 * 60 * 60 * 1000;

/** Trial duration in days (for UI display) */
export const TRIAL_DURATION_DAYS = 14;
